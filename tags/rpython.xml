<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>osa1.net - Posts tagged rpython</title>
    <link href="http://osa1.net/tags/rpython.xml" rel="self" />
    <link href="http://osa1.net" />
    <id>http://osa1.net/tags/rpython.xml</id>
    <author>
        <name>Ömer Sinan Ağacan</name>
        <email>omeragaca@gmail.com</email>
    </author>
    <updated>2015-01-29T00:00:00Z</updated>
    <entry>
    <title>Some benchmarks for meta-tracing BF JIT and traditional BF implementations</title>
    <link href="http://osa1.net/posts/2015-04-11-some-bf-benchmarks.html" />
    <id>http://osa1.net/posts/2015-04-11-some-bf-benchmarks.html</id>
    <published>2015-01-29T00:00:00Z</published>
    <updated>2015-01-29T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I found RPython very interesting for several reasons which I may be talking about later, and I need to use it for a project, so I started running some tutorials. However, I had some concerns about the idea(I still have, and I’ll defer the discussion to some other post for now), and I wanted to experiment with different implementations of same interpreter and compare results.</p>
<p>What I wanted to see is, given a very good and mature JIT compiler(LuaJIT in this case), how hard would it be to have similar optimizations without annotating code manually to give hints to the JIT compiler.</p>
<p>So I implemented a simple BF interpreter in Lua, and started experimenting with different optimizations. As for benchmarking, I used <code>bench</code> program from this <a href="https://bitbucket.org/brownan/pypy-tutorial/">RPython tutorial repository</a>.</p>
<p>Before diving into Lua implementation, here results of running them with RPython compiled interpreter, Python and PyPy:</p>
<pre><code>./example5-rpython bench.b    0.94s user 0.00s system 99% cpu 0.947 total
pypy example5.py   bench.b   15.57s user 0.01s system 99% cpu 15.597 total
python example5.py bench.b  597.34s user 0.04s system 99% cpu 9:57.87 total</code></pre>
<p>The Lua implementation started with this:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode lua"><code class="sourceCode lua"><a class="sourceLine" id="cb2-1" title="1"><span class="kw">function</span> mainloop<span class="op">(</span>program<span class="op">,</span> bracket_map<span class="op">,</span> dispatch_fn<span class="op">)</span></a>
<a class="sourceLine" id="cb2-2" title="2">    <span class="kw">local</span> pc <span class="op">=</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-3" title="3">    <span class="kw">local</span> tape <span class="op">=</span> <span class="op">{</span><span class="dv">0</span><span class="op">}</span></a>
<a class="sourceLine" id="cb2-4" title="4">    <span class="kw">local</span> tape_pos <span class="op">=</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-5" title="5"></a>
<a class="sourceLine" id="cb2-6" title="6">    <span class="kw">local</span> code</a>
<a class="sourceLine" id="cb2-7" title="7">    <span class="cf">while</span> pc <span class="op">&lt;=</span> <span class="op">#</span>program <span class="cf">do</span></a>
<a class="sourceLine" id="cb2-8" title="8">        code <span class="op">=</span> program<span class="op">[</span>pc<span class="op">]</span></a>
<a class="sourceLine" id="cb2-9" title="9">        <span class="cf">if</span> code <span class="op">==</span> <span class="st">&quot;&gt;&quot;</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-10" title="10">            tape_pos <span class="op">=</span> tape_pos <span class="op">+</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-11" title="11">            <span class="cf">if</span> <span class="op">#</span>tape <span class="op">&lt;</span> tape_pos <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-12" title="12">                <span class="fu">table.insert</span><span class="op">(</span>tape<span class="op">,</span> <span class="dv">0</span><span class="op">)</span></a>
<a class="sourceLine" id="cb2-13" title="13">            <span class="cf">end</span></a>
<a class="sourceLine" id="cb2-14" title="14">        <span class="cf">elseif</span> code <span class="op">==</span> <span class="st">&quot;&lt;&quot;</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-15" title="15">            tape_pos <span class="op">=</span> tape_pos <span class="op">-</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-16" title="16">        <span class="cf">elseif</span> code <span class="op">==</span> <span class="st">&quot;+&quot;</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-17" title="17">            tape<span class="op">[</span>tape_pos<span class="op">]</span> <span class="op">=</span> tape<span class="op">[</span>tape_pos<span class="op">]</span> <span class="op">+</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-18" title="18">        <span class="cf">elseif</span> code <span class="op">==</span> <span class="st">&quot;-&quot;</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-19" title="19">            tape<span class="op">[</span>tape_pos<span class="op">]</span> <span class="op">=</span> tape<span class="op">[</span>tape_pos<span class="op">]</span> <span class="op">-</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-20" title="20">        <span class="cf">elseif</span> code <span class="op">==</span> <span class="st">&quot;.&quot;</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-21" title="21">            <span class="fu">io.write</span><span class="op">(</span><span class="fu">string.char</span><span class="op">(</span>tape<span class="op">[</span>tape_pos<span class="op">]))</span></a>
<a class="sourceLine" id="cb2-22" title="22">        <span class="cf">elseif</span> code <span class="op">==</span> <span class="st">&quot;[&quot;</span> <span class="kw">and</span> tape<span class="op">[</span>tape_pos<span class="op">]</span> <span class="op">==</span> <span class="dv">0</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-23" title="23">            pc <span class="op">=</span> bracket_map<span class="op">[</span>pc<span class="op">]</span></a>
<a class="sourceLine" id="cb2-24" title="24">        <span class="cf">elseif</span> code <span class="op">==</span> <span class="st">&quot;]&quot;</span> <span class="kw">and</span> tape<span class="op">[</span>tape_pos<span class="op">]</span> <span class="op">~=</span> <span class="dv">0</span> <span class="cf">then</span></a>
<a class="sourceLine" id="cb2-25" title="25">            pc <span class="op">=</span> bracket_map<span class="op">[</span>pc<span class="op">]</span></a>
<a class="sourceLine" id="cb2-26" title="26">        <span class="cf">end</span></a>
<a class="sourceLine" id="cb2-27" title="27">        pc <span class="op">=</span> pc <span class="op">+</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb2-28" title="28">    <span class="cf">end</span></a>
<a class="sourceLine" id="cb2-29" title="29"><span class="cf">end</span></a></code></pre></div>
<p>I’m not sure how many reasonable different implementations one can come up with, given that the language is this small. Still, there are some optimizations that we can do and I’ve tried some of them. Here are some things I tried:</p>
<ul>
<li>I tried replacing one character strings with ASCII code equivalents. Since Lua doesn’t have character constants, I thought this may give us a few instructions per branch. But results were just the same.</li>
<li>I tried replacing table getters and setters with <code>rawget</code> and <code>rawset</code>s. Nothing changed. Apparently it’s not worth the effort unless you have a metatable for your table.</li>
<li><p>I tried generating a huge “if-then-else” statement for <code>bracket_map</code>, and used it as a jump table kind of thing. Here’s the code:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode lua"><code class="sourceCode lua"><a class="sourceLine" id="cb3-1" title="1"><span class="kw">function</span> gen_dispatch_fn<span class="op">(</span>bracket_map<span class="op">,</span> fun_name<span class="op">)</span></a>
<a class="sourceLine" id="cb3-2" title="2">    <span class="kw">local</span> first <span class="op">=</span> <span class="kw">true</span></a>
<a class="sourceLine" id="cb3-3" title="3">    <span class="kw">local</span> acc <span class="op">=</span> <span class="op">{}</span></a>
<a class="sourceLine" id="cb3-4" title="4">    <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;function &quot;</span> <span class="op">..</span> fun_name <span class="op">..</span> <span class="st">&quot;(arg)</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-5" title="5">    <span class="cf">for</span> k<span class="op">,</span>v <span class="kw">in</span> <span class="fu">pairs</span><span class="op">(</span>bracket_map<span class="op">)</span> <span class="cf">do</span></a>
<a class="sourceLine" id="cb3-6" title="6">        <span class="cf">if</span> first <span class="cf">then</span></a>
<a class="sourceLine" id="cb3-7" title="7">            <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;    if arg == &quot;</span> <span class="op">..</span> k <span class="op">..</span> <span class="st">&quot; then</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-8" title="8">            first <span class="op">=</span> <span class="kw">false</span></a>
<a class="sourceLine" id="cb3-9" title="9">        <span class="cf">else</span></a>
<a class="sourceLine" id="cb3-10" title="10">            <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;    elseif arg == &quot;</span> <span class="op">..</span> k <span class="op">..</span> <span class="st">&quot; then</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-11" title="11">        <span class="cf">end</span></a>
<a class="sourceLine" id="cb3-12" title="12">        <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;        return &quot;</span> <span class="op">..</span> v <span class="op">..</span> <span class="st">&quot;</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-13" title="13">    <span class="cf">end</span></a>
<a class="sourceLine" id="cb3-14" title="14">    <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;    else</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-15" title="15">    <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;        error(</span><span class="sc">\&quot;</span><span class="st">invalid arg: </span><span class="sc">\&quot;</span><span class="st"> .. arg)</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-16" title="16">    <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;    end</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-17" title="17">    <span class="fu">table.insert</span><span class="op">(</span>acc<span class="op">,</span> <span class="st">&quot;end</span><span class="sc">\n</span><span class="st">&quot;</span><span class="op">)</span></a>
<a class="sourceLine" id="cb3-18" title="18">    <span class="cf">return</span> <span class="fu">table.concat</span><span class="op">(</span>acc<span class="op">)</span></a>
<a class="sourceLine" id="cb3-19" title="19"><span class="cf">end</span></a></code></pre></div>
<p>I loaded this code using standard <code>load()</code> function. This also didn’t work. The reason is that, even if this is faster(which is probably not always the case), profiling showed that interpreter spents only 4% of the time for <code>bracket_map</code> lookups. So if this implementation only slightly faster, it just can’t make a big difference.</p></li>
</ul>
<p>Profiling output revealed that, 85% of the time spent on fetching the next instruction:</p>
<pre><code>@@ 69 @@
      |
      |     local code
      |     while pc &lt;= #program do
  85% |         code = program[pc]
      |         if code == 62 then
      |             tape_pos = tape_pos + 1
      |             if #tape &lt; tape_pos then
@@ 89 @@
      |             pc = bracket_map[pc]
      |         elseif code == 93 and tape[tape_pos] ~= 0 then
   4% |             pc = bracket_map[pc]
      |         end
      |         pc = pc + 1</code></pre>
<p>Which really means that you can’t optimize anything, because there’s nothing optimizable in <code>code = program[pc]</code>, since this is one of the most primitive operations that you can do in this language. (note that we don’t have metamethod assigned to this table, so <code>rawget</code> is not an optimization)</p>
<p>At this point the Lua results were like this:</p>
<pre><code>luajit example_lua.lua bench.b  34.41s user 0.00s system 99% cpu 34.442 total</code></pre>
<p>The fact that PyPy did better job than LuaJIT here is surprising and impressive. It seems like RPython and PyPy is doing a very good job here.</p>
<p>Since I already started gradually compiling things, I thought why not go further and compile everything. Here’s a simple BF to Lua compiler:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode lua"><code class="sourceCode lua"><a class="sourceLine" id="cb6-1" title="1"><span class="kw">function</span> compile<span class="op">(</span>str<span class="op">)</span></a>
<a class="sourceLine" id="cb6-2" title="2">    <span class="kw">local</span> pgm <span class="op">=</span> <span class="op">{}</span></a>
<a class="sourceLine" id="cb6-3" title="3">    <span class="fu">table.insert</span><span class="op">(</span>pgm<span class="op">,</span> <span class="vs">[[</span></a>
<a class="sourceLine" id="cb6-4" title="4"><span class="vs">function pgm()</span></a>
<a class="sourceLine" id="cb6-5" title="5"><span class="vs">    local tape = {0}</span></a>
<a class="sourceLine" id="cb6-6" title="6"><span class="vs">    local tape_pos = 1</span></a>
<a class="sourceLine" id="cb6-7" title="7"><span class="vs">]])</span></a>
<a class="sourceLine" id="cb6-8" title="8"></a>
<a class="sourceLine" id="cb6-9" title="9"><span class="vs">    local adv = [[</span></a>
<a class="sourceLine" id="cb6-10" title="10"><span class="vs">    tape_pos = tape_pos + 1</span></a>
<a class="sourceLine" id="cb6-11" title="11"><span class="vs">    if #tape &lt; tape_pos then</span></a>
<a class="sourceLine" id="cb6-12" title="12"><span class="vs">        table.insert(tape, 0)</span></a>
<a class="sourceLine" id="cb6-13" title="13"><span class="vs">    end</span></a>
<a class="sourceLine" id="cb6-14" title="14"><span class="vs">]]</span></a>
<a class="sourceLine" id="cb6-15" title="15"><span class="vs">    local function dev(i) return &quot;    tape_pos = tape_pos - &quot; .. i .. &quot;\n&quot; end</span></a>
<a class="sourceLine" id="cb6-16" title="16"><span class="vs">    local function inc(i) return &quot;    tape[tape_pos] = tape[tape_pos] + &quot; .. i .. &quot;\n&quot; end</span></a>
<a class="sourceLine" id="cb6-17" title="17"><span class="vs">    local function dec(i) return &quot;    tape[tape_pos] = tape[tape_pos] - &quot; .. i .. &quot;\n&quot; end</span></a>
<a class="sourceLine" id="cb6-18" title="18"><span class="vs">    local out  = &quot;    io.write(string.char(tape[tape_pos]))\n&quot;</span></a>
<a class="sourceLine" id="cb6-19" title="19"><span class="vs">    local inp  = &quot;&quot; -- no need for this</span></a>
<a class="sourceLine" id="cb6-20" title="20"><span class="vs">    local jmpF = &quot;    while tape[tape_pos] ~= 0 do\n&quot;</span></a>
<a class="sourceLine" id="cb6-21" title="21"><span class="vs">    local jmpB = &quot;    if tape[tape_pos] == 0 then break end end\n&quot;</span></a>
<a class="sourceLine" id="cb6-22" title="22"></a>
<a class="sourceLine" id="cb6-23" title="23"><span class="vs">    -- these are used to combine consecutive same instructions</span></a>
<a class="sourceLine" id="cb6-24" title="24"><span class="vs">    local devs = 0</span></a>
<a class="sourceLine" id="cb6-25" title="25"><span class="vs">    local incs = 0</span></a>
<a class="sourceLine" id="cb6-26" title="26"><span class="vs">    local decs = 0</span></a>
<a class="sourceLine" id="cb6-27" title="27"></a>
<a class="sourceLine" id="cb6-28" title="28"><span class="vs">    local indent = 0;</span></a>
<a class="sourceLine" id="cb6-29" title="29"></a>
<a class="sourceLine" id="cb6-30" title="30"><span class="vs">    for i=1, #str do</span></a>
<a class="sourceLine" id="cb6-31" title="31"><span class="vs">        local char = string.char(string.byte(str, i))</span></a>
<a class="sourceLine" id="cb6-32" title="32"></a>
<a class="sourceLine" id="cb6-33" title="33"><span class="vs">        if devs ~= 0 and char ~= &quot;&lt;&quot; then</span></a>
<a class="sourceLine" id="cb6-34" title="34"><span class="vs">            table.insert(pgm, indent_lines(indent, dev(devs)))</span></a>
<a class="sourceLine" id="cb6-35" title="35"><span class="vs">            devs = 0</span></a>
<a class="sourceLine" id="cb6-36" title="36"><span class="vs">        elseif incs ~= 0 and char ~= &quot;+&quot; then</span></a>
<a class="sourceLine" id="cb6-37" title="37"><span class="vs">            table.insert(pgm, indent_lines(indent, inc(incs)))</span></a>
<a class="sourceLine" id="cb6-38" title="38"><span class="vs">            incs = 0</span></a>
<a class="sourceLine" id="cb6-39" title="39"><span class="vs">        elseif decs ~= 0 and char ~= &quot;-&quot; then</span></a>
<a class="sourceLine" id="cb6-40" title="40"><span class="vs">            table.insert(pgm, indent_lines(indent, dec(decs)))</span></a>
<a class="sourceLine" id="cb6-41" title="41"><span class="vs">            decs = 0</span></a>
<a class="sourceLine" id="cb6-42" title="42"><span class="vs">        end</span></a>
<a class="sourceLine" id="cb6-43" title="43"></a>
<a class="sourceLine" id="cb6-44" title="44"><span class="vs">        if char == &quot;&gt;&quot; then -- 62</span></a>
<a class="sourceLine" id="cb6-45" title="45"><span class="vs">            table.insert(pgm, indent_lines(indent, adv))</span></a>
<a class="sourceLine" id="cb6-46" title="46"><span class="vs">        elseif char == &quot;&lt;&quot; then -- 60</span></a>
<a class="sourceLine" id="cb6-47" title="47"><span class="vs">            devs = devs + 1</span></a>
<a class="sourceLine" id="cb6-48" title="48"><span class="vs">        elseif char == &quot;+&quot; then -- 43</span></a>
<a class="sourceLine" id="cb6-49" title="49"><span class="vs">            incs = incs + 1</span></a>
<a class="sourceLine" id="cb6-50" title="50"><span class="vs">        elseif char == &quot;-&quot; then -- 45</span></a>
<a class="sourceLine" id="cb6-51" title="51"><span class="vs">            decs = decs + 1</span></a>
<a class="sourceLine" id="cb6-52" title="52"><span class="vs">        elseif char == &quot;.&quot; then -- 46</span></a>
<a class="sourceLine" id="cb6-53" title="53"><span class="vs">            table.insert(pgm, indent_lines(indent, out))</span></a>
<a class="sourceLine" id="cb6-54" title="54"><span class="vs">        elseif char == &quot;,&quot; then -- 44</span></a>
<a class="sourceLine" id="cb6-55" title="55"><span class="vs">            table.insert(pgm, indent_lines(indent, inp))</span></a>
<a class="sourceLine" id="cb6-56" title="56"><span class="vs">        elseif char == &quot;[&quot; then -- 91</span></a>
<a class="sourceLine" id="cb6-57" title="57"><span class="vs">            indent = indent + 4</span></a>
<a class="sourceLine" id="cb6-58" title="58"><span class="vs">            table.insert(pgm, indent_lines(indent, jmpF))</span></a>
<a class="sourceLine" id="cb6-59" title="59"><span class="vs">        elseif char == &quot;]&quot; then -- 93</span></a>
<a class="sourceLine" id="cb6-60" title="60"><span class="vs">            indent = indent - 4</span></a>
<a class="sourceLine" id="cb6-61" title="61"><span class="vs">            table.insert(pgm, indent_lines(indent, jmpB))</span></a>
<a class="sourceLine" id="cb6-62" title="62"><span class="vs">        end</span></a>
<a class="sourceLine" id="cb6-63" title="63"><span class="vs">    end</span></a>
<a class="sourceLine" id="cb6-64" title="64"></a>
<a class="sourceLine" id="cb6-65" title="65"><span class="vs">    table.insert(pgm, &quot;end&quot;)</span></a>
<a class="sourceLine" id="cb6-66" title="66"><span class="vs">    return table.concat(pgm)</span></a>
<a class="sourceLine" id="cb6-67" title="67"><span class="vs">end</span></a></code></pre></div>
<p>One thing to note here is that loops in BF programs correspond to loops in generated Lua. There’s another way to implement this compiler and it might turn out to be more efficient, but I didn’t try it. (see BF-to-C compiler below) Also, I’m merging some instructions together. This has significant performance impact, but it’s also necessary because if the generated code is too big, both PUC-Lua and LuaJIT is rejecting to load it. (this is documented, but the limit is not specified)</p>
<p>Results:</p>
<pre><code>luajit example_lua.lua bench.b  0.53s user 0.00s system 99% cpu 0.532 total</code></pre>
<p>Note that runtime code generation and loading is NOT included in this number, but code generation takes less than 0.01s, so I might just include that.</p>
<p>Just for completeness, I also tried a <a href="https://github.com/kgabis/brainfuck-c/blob/master/brainfuck.c">C interpreter</a>, and <a href="http://awk.info/?doc/bfc.html">BF-to-C</a> compiler:</p>
<pre><code>./c-int bench.b  2.44s user 0.00s system 99% cpu 2.443 total
./c-compiled     0.00s user 0.00s system 82% cpu 0.004 total</code></pre>
<p>A fun thing about C compiler is that compiling generated C programs takes long time:</p>
<pre><code>gcc -O3 awk_output.c  14.07s user 0.14s system 99% cpu 14.219 total</code></pre>
<p>RPython once again does an impressive job here, because it’s even faster than C interpreter. I didn’t bother profiling C code and optimizing it, because it looks like a reasonable implementation: A simple “fetch instruction and run it in a case statement” loop.</p>
<p>So I think the conclusion is that RPython and PyPy are doing really good job.</p>]]></summary>
</entry>

</feed>
