<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>osa1.net - Posts tagged parsing</title>
    <link href="http://osa1.net/tags/parsing.xml" rel="self" />
    <link href="http://osa1.net" />
    <id>http://osa1.net/tags/parsing.xml</id>
    <author>
        <name>Ömer Sinan Ağacan</name>
        <email>omeragaca@gmail.com</email>
    </author>
    <updated>2015-01-29T00:00:00Z</updated>
    <entry>
    <title>Top-down expression parsing is easy</title>
    <link href="http://osa1.net/posts/2015-01-29-top-down-expr-parsing-easy.html" />
    <id>http://osa1.net/posts/2015-01-29-top-down-expr-parsing-easy.html</id>
    <published>2015-01-29T00:00:00Z</published>
    <updated>2015-01-29T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I recently fixed <a href="http://hackage.haskell.org/package/language-lua">language-lua</a>’s 2-years-old expression parsing bug. Previously it was using <a href="http://hackage.haskell.org/package/parsec-3.1.8/docs/Text-Parsec-Expr.html">Parsec’s expression parser</a>, which is actually horrible because it can’t handle chained unary operators.</p>
<p>Two weeks ago I decided to take a look into Lua’s original implementation, and in about an hour or so the algorithm was crystal clear to me. I immediately <a href="https://github.com/osa1/language-lua/commit/b4bebe36e927dcc671dbe6dd19572b83073dc556#diff-630bbd2d118baf109da6ad79d3f168bfR257">implemented it</a> and closed the <a href="https://github.com/osa1/language-lua/issues/2">2-years-old bug report</a>.</p>
<p>This implementation is essentially a port of Lua’s expression parser. Recently I thought about the algorithm and I was wondering if this has a name – the algorithm looked pretty obvious to me once I understand and given how much we know about parsing I thought this should have a name.</p>
<p>I found <a href="http://www.engr.mun.ca/~theo/Misc/exp_parsing.htm#climbing">this algorithm named “precedence climbing”</a>. This is almost the same algorithm, only difference is that instead of using <code>lookahead</code> I’m just consuming the binary operator and returning it to the caller(which is parsing an expression with lower precedence than current parser) if precedence is lower. Associativity handling is also different(I use different left and right precedences to handle associativity) but the idea is really the same.</p>
<p>Now, there is also another algorithm called Pratt, and I can’t read the original paper(paywall), but according to <a href="http://lambda-the-ultimate.org/node/3682">this LtU discussion</a> it should also be similar. Indeed, <a href="http://journal.stuffwithstuff.com/2011/03/19/pratt-parsers-expression-parsing-made-easy/">this explanation of it</a> looks pretty similar, and <a href="http://stackoverflow.com/a/13637731/691032">this StackOverflow answer</a> says that Lua’s implementation is “Pratt style parsing”.</p>
<p>So it seems like we have two, or maybe one since they’re actually very similar, solution(s) to solve top-down expression parsing problem and Haskell implementation using Parsec is possible in only 12 lines of code.</p>
<h1 id="a-challenge">A challenge</h1>
<p>One challenge might be to modify Parsec’s expression parser so that internally it generates a Pratt/precedence climbing parser. I’m hoping to spare some time to work on this.</p>]]></summary>
</entry>
<entry>
    <title>An idea to handle left-recursion in Parsec</title>
    <link href="http://osa1.net/posts/2014-03-07-parsec-left-recursion.html" />
    <id>http://osa1.net/posts/2014-03-07-parsec-left-recursion.html</id>
    <published>2014-03-07T00:00:00Z</published>
    <updated>2014-03-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I recently realized that it may be possible to handle left-recursion in Parsec style parser combinator libraries. I quickly wrote a simple prototype implementation that demonstrates the idea.</p>
<p>The idea is to keep track of parser functions that are called without consuming any tokens from input stream. You should be able to run failure procedures when same parser function is encountered more than one time without consuming any tokens. When a token is consumed, state that is used to keep track of parser functions should be reset.</p>
<p>One assumption here is that I’m assuming parser functions do not alter any states. Otherwise when you come across a same parser, you can have different state and parser may behave differently.</p>
<p>Implementing the idea is easy even without having any extra support from Parsec. Here’s a demonstration on my favorite ambiguous grammar:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="co">-- Exp ::= Int</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="co">--      |  Exp `+` Exp</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="kw">data</span> <span class="dt">Exp</span> <span class="fu">=</span> <span class="dt">Int</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">         <span class="fu">|</span> <span class="dt">Add</span> <span class="dt">Exp</span> <span class="dt">Exp</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">         <span class="kw">deriving</span> (<span class="dt">Show</span>)</a></code></pre></div>
<p>Parsers for non-terminals are easy:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1">plus <span class="fu">=</span> char <span class="ch">&#39;+&#39;</span> <span class="fu">&gt;&gt;</span> spaces</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">int  <span class="fu">=</span> fmap (<span class="dt">Int</span> <span class="fu">.</span> read) <span class="fu">$</span> many1 digit <span class="fu">&lt;*</span> spaces</a></code></pre></div>
<p>I’m using a set to keep track of already visited parsers:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">type</span> <span class="dt">MarksSeen</span> <span class="fu">=</span> <span class="dt">S.Set</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2"></a>
<a class="sourceLine" id="cb3-3" data-line-number="3"><span class="kw">type</span> <span class="dt">RecParser</span> s m a <span class="fu">=</span> <span class="dt">ParsecT</span> s <span class="dt">MarksSeen</span> m a</a></code></pre></div>
<p>Auxiliary functions to alter the state that keeps track of visited parsers:</p>
<pre><code>putMark i = do
    is &lt;- getState
    if S.member i is
      then fail &quot;recursion&quot;
      else putState $ S.insert i is

resetMarks = putState S.empty</code></pre>
<p>Now the interesting part, <code>add</code> parser marks itself as first thing to do, and calls <code>exp</code> parser. Since <code>exp</code> parser is entry point, this means an indirect recursive call. When same <code>putMark</code> call is made, Parsec runs failure actions instead of going into infinte loop:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1">add <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2">    putMark <span class="dv">1</span></a>
<a class="sourceLine" id="cb5-3" data-line-number="3">    e1 <span class="ot">&lt;-</span> exp</a>
<a class="sourceLine" id="cb5-4" data-line-number="4">    resetMarks</a>
<a class="sourceLine" id="cb5-5" data-line-number="5">    plus</a>
<a class="sourceLine" id="cb5-6" data-line-number="6">    e2 <span class="ot">&lt;-</span> exp</a>
<a class="sourceLine" id="cb5-7" data-line-number="7">    spaces</a>
<a class="sourceLine" id="cb5-8" data-line-number="8">    return <span class="fu">$</span> <span class="dt">Add</span> e1 e2</a>
<a class="sourceLine" id="cb5-9" data-line-number="9"></a>
<a class="sourceLine" id="cb5-10" data-line-number="10">exp <span class="fu">=</span> choice [try add, int]</a></code></pre></div>
<p><code>resetMarks</code> call is also important, <code>exp</code> has to consume some tokens, so after parsing <code>e1</code>, I’m calling <code>resetMarks</code>.</p>
<p>Here’s an example call of this parser:</p>
<pre><code>ghci&gt; runParser exp S.empty &quot;&quot; &quot;1 + 2 + 3 + 4&quot;
Right (Add (Int 1) (Add (Int 2) (Add (Int 3) (Int 4))))</code></pre>
<p>You can observe that parser gets into an infinite loop when marks are removed. Here’s an example demonstrating the error message:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1">rec <span class="fu">=</span> putMark <span class="dv">0</span> <span class="fu">&gt;&gt;</span> rec <span class="fu">&gt;&gt;</span> resetMarks</a>
<a class="sourceLine" id="cb7-2" data-line-number="2"></a>
<a class="sourceLine" id="cb7-3" data-line-number="3">ghci<span class="fu">&gt;</span> runParser rec S.empty <span class="st">&quot;&quot;</span> <span class="st">&quot;&quot;</span></a>
<a class="sourceLine" id="cb7-4" data-line-number="4"><span class="dt">Left</span> (line <span class="dv">1</span>, column <span class="dv">1</span>)<span class="fu">:</span></a>
<a class="sourceLine" id="cb7-5" data-line-number="5">recursion</a></code></pre></div>
<p>One problem with this approach is that it requires more typing, and you should be careful too. Marks can be placed using TemplateHaskell to ensure unique numbers are given to each <code>putMark</code> call. As a second improvement, I think with some modifications on Parsec we can make Parsec to reset marks when a token is consumed(using <em>consumed-ok</em> continuation of <code>ParsecT</code>).</p>
<p>You can see the complete program <a href="https://gist.github.com/osa1/9414577">here</a>.</p>
<hr />
<p>Removing left-recursions in your grammar may not be a huge problem – except when you’re working on functional languages with ML-like syntax. Then you’re out of luck because being functional means you’re <code>Exp</code> non-terminal contains several dozen of productions and function applications is a part of that too, and it’s left-recursive:</p>
<pre><code>Exp ::= ...
     |  Exp Exp_1 ... Exp_N [left-associative]
     ... a hundred more productions ...</code></pre>
<p>Whenever I need to write a parser for a grammar like this, I’m thinking for an easier way to parse it. I still couldn’t come up with a solution. Idea I just explained does not solve it, because it parses right-associatively. There is one workaround, but I’m not sure if that results with a parser for same grammar:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1">app <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-2" data-line-number="2">    putMark <span class="dv">2</span></a>
<a class="sourceLine" id="cb9-3" data-line-number="3">    fn <span class="ot">&lt;-</span> exp</a>
<a class="sourceLine" id="cb9-4" data-line-number="4">    resetMarks</a>
<a class="sourceLine" id="cb9-5" data-line-number="5"></a>
<a class="sourceLine" id="cb9-6" data-line-number="6">    putMark <span class="dv">2</span></a>
<a class="sourceLine" id="cb9-7" data-line-number="7">    as <span class="ot">&lt;-</span> many1 exp</a>
<a class="sourceLine" id="cb9-8" data-line-number="8">    resetMarks</a>
<a class="sourceLine" id="cb9-9" data-line-number="9"></a>
<a class="sourceLine" id="cb9-10" data-line-number="10">    return <span class="fu">$</span> foldl <span class="dt">App</span> fn as</a>
<a class="sourceLine" id="cb9-11" data-line-number="11"></a>
<a class="sourceLine" id="cb9-12" data-line-number="12"></a>
<a class="sourceLine" id="cb9-13" data-line-number="13">ghci<span class="fu">&gt;</span> runParser pgm S.empty <span class="st">&quot;&quot;</span> <span class="st">&quot;1 2 3 4 + 5 + 6 7&quot;</span></a>
<a class="sourceLine" id="cb9-14" data-line-number="14"><span class="dt">Right</span> (<span class="dt">Add</span> (<span class="dt">App</span> (<span class="dt">App</span> (<span class="dt">App</span> (<span class="dt">Int</span> <span class="dv">1</span>) (<span class="dt">Int</span> <span class="dv">2</span>)) (<span class="dt">Int</span> <span class="dv">3</span>)) (<span class="dt">Int</span> <span class="dv">4</span>)) (<span class="dt">Add</span> (<span class="dt">Int</span> <span class="dv">5</span>) (<span class="dt">App</span> (<span class="dt">Int</span> <span class="dv">6</span>) (<span class="dt">Int</span> <span class="dv">7</span>))))</a>
<a class="sourceLine" id="cb9-15" data-line-number="15"></a>
<a class="sourceLine" id="cb9-16" data-line-number="16">ghci<span class="fu">&gt;</span> runParser pgm S.empty <span class="st">&quot;&quot;</span> <span class="st">&quot;1 + 2 + 3 4&quot;</span></a>
<a class="sourceLine" id="cb9-17" data-line-number="17"><span class="dt">Right</span> (<span class="dt">Add</span> (<span class="dt">Int</span> <span class="dv">1</span>) (<span class="dt">Add</span> (<span class="dt">Int</span> <span class="dv">2</span>) (<span class="dt">App</span> (<span class="dt">Int</span> <span class="dv">3</span>) (<span class="dt">Int</span> <span class="dv">4</span>))))</a></code></pre></div>]]></summary>
</entry>
<entry>
    <title>Memoized parsing in continuation-passing style</title>
    <link href="http://osa1.net/posts/2013-10-21-memoized-parsing-cps.html" />
    <id>http://osa1.net/posts/2013-10-21-memoized-parsing-cps.html</id>
    <published>2013-10-21T00:00:00Z</published>
    <updated>2013-10-21T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Continuations are truly magical things. They’re the “ultimate abstractions of control flow”. Even without using any fancy language features like <code>call/cc</code>, you can have seriously cool and mind-boggling programs.</p>
<p>In <a href="http://arxiv.org/pdf/cmp-lg/9504016">“Memoization in Top-Down Parsing”</a> paper, Mark Johnson builds up from memoizing top-down parsers and describes a way to handle left recursion in top-down parsers by combining memoization techniques with continuations.</p>
<p>I ported the code to Lua to experiment, you can see it <a href="https://gist.github.com/osa1/7089333">here</a>. Most interesting part is the memoized CPS parser generator from a normal CPS parser function:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode lua"><code class="sourceCode lua"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">function</span> memo<span class="ot">(</span>parser<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">    <span class="do">-- </span><span class="al">WARNING</span><span class="do">: this function is badly implemented in the sense that</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    <span class="do">-- if you parser generated by this function on two different streams</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    <span class="do">-- it will generate wrong results</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">    <span class="kw">local</span> tbl <span class="ot">=</span> <span class="ot">{}</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">    <span class="kw">return</span> <span class="kw">function</span> <span class="ot">(</span>stream<span class="ot">,</span> idx<span class="ot">,</span> cont<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7">        <span class="kw">if</span> tbl<span class="ot">[</span>idx<span class="ot">]</span> <span class="ot">==</span> <span class="kw">nil</span> <span class="kw">then</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8">            tbl<span class="ot">[</span>idx<span class="ot">]</span> <span class="ot">=</span> <span class="ot">{</span> results <span class="ot">=</span> <span class="ot">{},</span> conts <span class="ot">=</span> <span class="ot">{}</span> <span class="ot">}</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">            <span class="fu">table.insert</span><span class="ot">(</span>tbl<span class="ot">[</span>idx<span class="ot">].</span>conts<span class="ot">,</span> cont<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-10" data-line-number="10">            parser<span class="ot">(</span>stream<span class="ot">,</span> idx<span class="ot">,</span> <span class="kw">function</span> <span class="ot">(</span>parse_result<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11">                <span class="do">-- check if same parse_result is already in the table</span></a>
<a class="sourceLine" id="cb1-12" data-line-number="12">                <span class="kw">local</span> exists <span class="ot">=</span> <span class="kw">false</span></a>
<a class="sourceLine" id="cb1-13" data-line-number="13">                <span class="kw">for</span> _<span class="ot">,</span> result <span class="kw">in</span> <span class="fu">ipairs</span><span class="ot">(</span>tbl<span class="ot">[</span>idx<span class="ot">].</span>results<span class="ot">)</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-14" data-line-number="14">                    <span class="kw">if</span> result <span class="ot">==</span> parse_result <span class="kw">then</span> <span class="do">-- </span><span class="al">TODO</span><span class="do">: this equality is probably wrong</span></a>
<a class="sourceLine" id="cb1-15" data-line-number="15">                        exists <span class="ot">=</span> <span class="kw">true</span></a>
<a class="sourceLine" id="cb1-16" data-line-number="16">                        <span class="kw">break</span></a>
<a class="sourceLine" id="cb1-17" data-line-number="17">                    <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-18" data-line-number="18">                <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-19" data-line-number="19"> </a>
<a class="sourceLine" id="cb1-20" data-line-number="20">                <span class="kw">if</span> <span class="kw">not</span> exists <span class="kw">then</span></a>
<a class="sourceLine" id="cb1-21" data-line-number="21">                    <span class="fu">table.insert</span><span class="ot">(</span>tbl<span class="ot">[</span>idx<span class="ot">].</span>results<span class="ot">,</span> parse_result<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-22" data-line-number="22">                    <span class="kw">for</span> _<span class="ot">,</span> cont <span class="kw">in</span> <span class="fu">ipairs</span><span class="ot">(</span>tbl<span class="ot">[</span>idx<span class="ot">].</span>conts<span class="ot">)</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-23" data-line-number="23">                        cont<span class="ot">(</span>parse_result<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-24" data-line-number="24">                    <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-25" data-line-number="25">                <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-26" data-line-number="26">            <span class="kw">end</span><span class="ot">)</span></a>
<a class="sourceLine" id="cb1-27" data-line-number="27">        <span class="kw">else</span></a>
<a class="sourceLine" id="cb1-28" data-line-number="28">            <span class="fu">table.insert</span><span class="ot">(</span>tbl<span class="ot">[</span>idx<span class="ot">].</span>conts<span class="ot">,</span> cont<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-29" data-line-number="29">            <span class="kw">for</span> _<span class="ot">,</span> result <span class="kw">in</span> <span class="fu">ipairs</span><span class="ot">(</span>tbl<span class="ot">[</span>idx<span class="ot">].</span>results<span class="ot">)</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-30" data-line-number="30">                cont<span class="ot">(</span>result<span class="ot">)</span></a>
<a class="sourceLine" id="cb1-31" data-line-number="31">            <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-32" data-line-number="32">        <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-33" data-line-number="33">        <span class="kw">return</span> tbl<span class="ot">[</span>idx<span class="ot">]</span></a>
<a class="sourceLine" id="cb1-34" data-line-number="34">    <span class="kw">end</span></a>
<a class="sourceLine" id="cb1-35" data-line-number="35"><span class="kw">end</span></a></code></pre></div>
<p>(btw, I found porting this code to a purely functional setting very hard thing to do. If you find a way to do this, please send me your code. Thanks.)</p>
<p>This piece of code didn’t make sense to me for a while. I think the key to understand this function is to find answer to this question:</p>
<p>How is this different from keeping a set of productions visited without consuming any input from input stream and when you come to the same production, just failing instead of trying to parse? Because trying to derive same production without consuming any input means you’ll end up with infinite loop.</p>
<p>This function different in that it accounts for parsing same production after following a different path of production. Think this CFG as an example:</p>
<pre><code>T ::= T + T
    | int</code></pre>
<p>In order to derive first production, it first needs to parse a <code>T</code>. But then it will be already noted that it was already trying to parse <code>T</code>, and add the continuation to the list of continuations to be called when a T at input position 1 is parsed.</p>
<p>While trying alternatives, it will parse an <code>int</code>, and derive <code>T -&gt; int</code> at input position 1. And since it had saved the continuations to call when it successfully parse a <code>T</code> at location 1, it will call this continuations and thus parsing will continue.</p>
<p>I hope this helps other people to understand the trick.</p>]]></summary>
</entry>

</feed>
